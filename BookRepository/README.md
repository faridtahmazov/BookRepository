I these learned while i write the app

Persistence.xml,
Jpa configuration,
Hibernate Configuration,
EntityManagerFactory,
EntityManager,
EntityTransaction,
Persist, Merge, Remove, Find

@Table, @Column, @Entity, @Temperal, @GeneratedValue @JoinColumn
